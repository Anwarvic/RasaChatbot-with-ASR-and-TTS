##### YOU CAN CHANGE THESE #####
# set True if cuda is available
cuda: False
# either 'greedy' or 'beam'... use 'beam' when you have a language model
decoder: 'greedy'
# absolute path to the trained ASR model
model_path: '/media/anwar/E/Chatbot/Rasa/asr/models/librispeech/librispeech_pretrained_v2.pth'

##### IN CASE OF 'beam' DECODING #####
lm_path: None
beam_width: 10
alpha: 0.8
beta: 1
cutoff_prob: 1.0
cutoff_top_n: 40
lm_workers: 1
half: False
offsets: False
top_paths: 1

